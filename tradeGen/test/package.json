{
  "name": "test-server",
  "version": "1.0.0",
  "description": "Test HTTP server for stock exchange simulator",
  "main": "testServer.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ken Tune",
  "license": "ISC",
  "dependencies": {
    "concat-stream": "^2.0.0"
  }
}
